@model IEnumerable<trilha_net_MVC_desafio_Entity.Models.Tarefa> //Indicando quem vai ser a model dessa página => "Carregue a página e tome aquela lista de contatos"


@{
    ViewData["Title"] = "Página Inicial";
}

<h2 class="mt-5" style="color: #1F879C">Lista de Tarefas</h2>
<p>Gerencie aqui suas tarefas! Mais organização no seu dia a dia 😉</p>
<div class="d-flex justify-content-between w-25">
    <p>
        <a asp-action="Criar" role="button" class="btn btn-info">Nova tarefa</a> @* asp-action: redirecionamento para
            uma página *@
    </p>

    <p>
        <a asp-action="BuscaDetalhada" role="button" class="btn btn-info">Realizar busca detalhada</a>
    </p>
</div>

<table class="table table-striped align-middle mt-4">
    <thead class="bg-dark bg-gradient text-white text-center">
        <tr>
            <th class="p-3">@Html.DisplayNameFor(model => model.Id)</th>
            <th class="p-3">@Html.DisplayNameFor(model => model.Titulo)</th>
            <th class="p-3">@Html.DisplayNameFor(model => model.Descricao)</th>
            <th class="p-3">@Html.DisplayNameFor(model => model.Status)</th>
            <th class="p-3">@Html.DisplayNameFor(model => model.Data)</th>
            <th class="p-3">Ações</th>
        </tr>
    </thead>
    <tbody class="text-center">
        @foreach (var item in Model) // para cada elemento que você recebeu na Model
        {
            <tr>
                <td>
                    @Html.DisplayFor(model => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Titulo)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Descricao)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Status)
                </td>
                <td>
                    @Html.DisplayFor(model => item.Data)
                </td>
                <td>
                    <a asp-action="Atualizar" role="button" class="btn btn-warning" asp-route-id="@item.Id">Atualizar</a> @*
                        Atualizar *@
                    <a asp-action="Deletar" role="button" class="btn btn-danger" asp-route-id="@item.Id">Deletar</a>
                </td>
            </tr>
        }
    </tbody>
</table>